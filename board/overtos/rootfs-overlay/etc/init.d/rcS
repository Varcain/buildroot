#!/bin/sh
# oveRTOS minimal rcS: the inittab handles the essential mounts, the SSH host-key
# generation, and starting inetd/getty, so the boot goes straight to the login
# shell. (The stock Buildroot S-scripts target services we do not have.)
echo "oveRTOS: rcS done"
